/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Remove deprecated appDir option - Next.js 14 doesn't need it
}

export default nextConfig

